process:
  process-type: 'string-interpolated-cmd'
  cmd: 'echo {template} {par_one} {par_two} {output}'
publisher:
  publisher-type: 'process-attr-pub'
  outputmap:
    signal_sample: output
environment:
  environment-type: 'docker-encapsulated'
  image: busybox
